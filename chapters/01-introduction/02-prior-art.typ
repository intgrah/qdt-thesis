== Prior art

Query-based incrementality has been applied to conventional compilers: rust-analyzer @matklad2020rust_analyzer uses the Salsa framework @salsa2018 for incremental type checking of Rust. In these languages, the compilation boundary is the type signature; changing a function body without changing its type cannot affect downstream checking. Fredriksson's Sixty @fredriksson2019sixty applies query-based incrementality to a dependently typed language, but does not formalise the underlying build system or verify that incremental results agree with batch elaboration. No mainstream proof assistant uses query-based incrementality.

Mokhov, Mitchell, and Peyton Jones @mokhov2018build show that build systems can be characterised by a single polymorphic type, the _Task_, parameterised by the class of effects a task is allowed to perform. _Shake_ supports dynamic dependencies (discovered during execution rather than declared up front) and _early cutoff_ (skipping dependents when a recomputed result is unchanged). The elaborator discovers which definitions it must unfold only as it runs, and early cutoff prevents unnecessary re-elaboration when an intermediate result has not changed. These are the properties we need.
