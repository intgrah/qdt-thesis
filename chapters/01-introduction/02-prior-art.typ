== Prior art

Query-based incrementality has been applied to conventional compilers: rust-analyzer @matklad2020rust_analyzer uses the Salsa framework @salsa2018 for incremental type checking of Rust. In these languages, the compilation boundary is the type signature --- changing a function body without changing its type cannot affect downstream checking. Fredriksson's Sixten (later Sixty) @fredriksson2019sixty applies query-based incrementality to a dependently typed language. It demonstrates viability, but does not formalise the underlying build system or verify that incremental results agree with batch elaboration. No mainstream proof assistant --- Lean, Agda, or Rocq --- uses query-based incrementality.

The theoretical foundation for fine-grained recomputation is _self-adjusting computation_ @acar2002adaptive, which tracks data dependencies at runtime and propagates changes through a dependency graph. Adapton @hammer2014adapton refines this with demand-driven evaluation. The "Build Systems à la Carte" framework applies these ideas at the granularity of named build targets rather than individual memory cells.

In a different guise, this is what build systems do. Mokhov et al. @mokhov2018build show that build systems such as Make, Shake, and Bazel are instances of a single polymorphic type, parameterised by the class of effects a task is allowed to perform. _Shake_ supports dynamic dependencies --- discovered during execution rather than declared up front --- and _early cutoff_ --- skipping dependents when a recomputed result is unchanged. These are the properties needed: the elaborator discovers which definitions it must unfold only as it runs, and early cutoff prevents unnecessary re-elaboration when an intermediate result has not changed.
