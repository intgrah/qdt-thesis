== Motivation

Lean's mathlib library @mathlib2020 contains over 130,000 definitions and 270,000 theorems.#footnote[#link("https://leanprover-community.github.io/mathlib_stats.html"), as of 2026-05-14.] Most of the re-elaboration triggered by an edit is wasted: mainstream proof assistants track dependencies at the granularity of module imports, so an edit invalidates everything its file transitively imports, even when no declaration in that closure actually unfolded the change. A finer scheme would record, for each declaration, the constants its elaboration actually unfolded; this set cannot be precomputed from the syntax, because a dependent type theory's _conversion rule_ decides at runtime which earlier definitions to unfold. Existing systems approximate at varying granularities: Wenzel's Isabelle/PIDE @wenzel2014pide and Lean's snapshot trees @ullrich2023extensible go to command-suffix within a file; Sixty @fredriksson2019sixty goes to per-declaration in memory within a language-server session.

Whatever the granularity, the incremental cache must agree with batch elaboration: a cache hit returning a value the elaborator's source code would not have produced is undetectable from interaction alone. Precise invalidation requires recording the cross-declaration dependencies a conversion check discovers at runtime, which extends the trusted layer to include that record. Correctness requires that a cache hit return what a fresh build would. No deployed system has met both.
